{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Start Node Server",
      "program": "${workspaceFolder}/09.react_routing_example/server.js"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "Open in Browser",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ],
  "compounds": [
    {
      "name": "Run Server and Browser",
      "configurations": ["Start Node Server", "Open in Browser"]
    }
  ]
}
